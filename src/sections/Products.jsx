import { motion } from 'framer-motion';import SectionTitle from '../components/SectionTitle';import ProductCard from '../components/ProductCard';import { products } from '../data/products';
export default function Products(){return <section id="sabores" className="scroll-mt-20 bg-[#FAF4ED] py-24"><div className="mx-auto max-w-7xl px-5 lg:px-8"><SectionTitle eyebrow="Nosso cardápio" title="Escolha o seu brownie" description="Dos clássicos aos bem recheados: encontre o sabor perfeito para o seu momento."/><motion.div initial="hidden" whileInView="show" viewport={{once:true,amount:.1}} variants={{show:{transition:{staggerChildren:.08}}}} className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{products.map(p=><motion.div key={p.id} variants={{hidden:{opacity:0,y:25},show:{opacity:1,y:0}}}><ProductCard product={p}/></motion.div>)}</motion.div></div></section>}
