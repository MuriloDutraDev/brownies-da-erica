import { useState } from 'react';import { AnimatePresence,motion } from 'framer-motion';import { Menu,X } from 'lucide-react';import Button from '../components/Button';import { CONTACT } from '../constants/contact';import { createWhatsAppLink } from '../utils/whatsapp';
const links=[['Início','#inicio'],['Sabores','#sabores'],['Sobre','#sobre'],['Como pedir','#como-pedir']];
function Logo(){return <a href="#inicio" className="leading-none"><span className="block font-script text-3xl text-espresso">Brownies</span><span className="block text-[9px] font-bold uppercase tracking-[.3em] text-caramel">da Érica</span></a>}
export default function Header(){const [open,setOpen]=useState(false);return <header className="fixed inset-x-0 top-0 z-40 border-b border-beige/50 bg-warm/90 backdrop-blur-lg"><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5 lg:px-8"><Logo/><nav className="hidden items-center gap-7 lg:flex">{links.map(([label,href])=><a key={href} href={href} className="text-sm font-semibold text-brown/75 hover:text-espresso">{label}</a>)}<a href={CONTACT.instagram} target="_blank" rel="noreferrer" className="text-sm font-semibold text-brown/75 hover:text-espresso">Instagram</a><Button href={createWhatsAppLink('Olá, Érica! Gostaria de fazer um pedido.')}>Fazer pedido</Button></nav><button onClick={()=>setOpen(!open)} className="rounded-full p-3 text-espresso lg:hidden" aria-label="Abrir menu" aria-expanded={open}>{open?<X/>:<Menu/>}</button></div><AnimatePresence>{open&&<motion.nav initial={{height:0,opacity:0}} animate={{height:'auto',opacity:1}} exit={{height:0,opacity:0}} className="overflow-hidden border-t border-beige/50 bg-warm px-5 lg:hidden"><div className="flex flex-col gap-1 py-4">{links.map(([label,href])=><a onClick={()=>setOpen(false)} key={href} href={href} className="rounded-xl px-3 py-3 font-semibold text-espresso">{label}</a>)}<a href={CONTACT.instagram} target="_blank" rel="noreferrer" className="rounded-xl px-3 py-3 font-semibold text-espresso">Instagram</a><Button href={createWhatsAppLink('Olá, Érica! Gostaria de fazer um pedido.')} className="mt-2 w-full">Fazer pedido</Button></div></motion.nav>}</AnimatePresence></header>}
