import { Image as ImageIcon } from 'lucide-react';
export default function ImagePlaceholder({label,src,className=''}){if(src)return <div className={`relative min-h-56 overflow-hidden rounded-[2rem] bg-beige ${className}`}><img src={src} alt={label} className="h-full w-full object-cover transition duration-500 group-hover:scale-105" loading="lazy"/></div>;return <div role="img" aria-label={label} className={`image-placeholder relative flex min-h-56 items-center justify-center overflow-hidden rounded-[2rem] border border-white/70 ${className}`}><div className="relative z-10 flex max-w-52 flex-col items-center text-center text-brown/65"><span className="mb-3 rounded-full bg-warm/80 p-3"><ImageIcon size={22}/></span><span className="text-xs font-bold uppercase tracking-[.16em]">{label}</span></div></div>}
