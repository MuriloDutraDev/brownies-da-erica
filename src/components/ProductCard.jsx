import { motion } from 'framer-motion';import Button from './Button';import ImagePlaceholder from './ImagePlaceholder';import { createWhatsAppLink } from '../utils/whatsapp';
export default function ProductCard({product}){return <motion.article whileHover={{y:-8}} transition={{duration:.25}} className="group flex h-full flex-col rounded-[2rem] bg-warm p-3 shadow-soft"><ImagePlaceholder src={product.image} label={product.alt} className="aspect-[4/3] min-h-0 rounded-[1.45rem]"/><div className="flex flex-1 flex-col p-4"><p className="mb-2 text-[11px] font-bold uppercase tracking-[.15em] text-accent">{product.category}</p><h3 className="font-serif text-2xl leading-tight text-espresso">{product.name}</h3><p className="mb-5 mt-3 flex-1 text-sm leading-relaxed text-brown">{product.description}</p><div className="mb-4 flex items-center justify-between border-t border-beige/70 pt-4"><span className="text-xs font-bold uppercase tracking-wider text-brown">A partir de</span><strong className="font-serif text-2xl text-espresso">{product.price}</strong></div><Button href={createWhatsAppLink(`Olá, Érica! Gostaria de pedir o brownie ${product.name}.`)} className="w-full">Pedir este sabor</Button></div></motion.article>}
